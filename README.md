# Project Repository

This repository contains the FitBuddy Android app project.

Build/run notes:
- The Android app lives under fitbuddy_mobile_app/.
- You can now run the Gradle wrapper from the repo root (proxy):
  - ./gradlew build
- Or use the wrapper from the app folder:
  - cd fitbuddy_mobile_app
  - ./gradlew build